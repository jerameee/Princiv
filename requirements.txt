# Core runtime
neo4j>=5.0.0
langgraph>=1.1.0
rdflib>=7.0

# NLP extraction pipeline (Task 1.7)
gliner>=0.2
spacy>=3.7

# veridian-core ingestion/storage abstractions
# Install separately during development:
#   pip install -e path/to/veridian-core
# or pin to a branch:
#   pip install git+https://github.com/jerameee/veridian-core.git@claude/review-bot-repos-oF8T4

# Testing
pytest>=9.0.0
pytest-asyncio>=0.23
